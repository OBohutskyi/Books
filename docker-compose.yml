version: '3.8'

services:
  sqlite3:
    image: postgres
  web:
    build: .
    command: python manage.py runserver 127.0.0.1:8000
    volumes:
      - .:/workdir
    ports:
      - "8000:8000"
    depends_on:
      - sqlite3